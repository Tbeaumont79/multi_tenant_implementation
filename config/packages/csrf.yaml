# Enable stateless CSRF protection for forms and logins/logouts
framework:
    form:
        csrf_protection:
            token_id: submit

    csrf_protection:
        stateless_token_ids:
            # - submit  # session-based: Symfony Forms (EasyAdmin) need this
            - authenticate
            - logout
